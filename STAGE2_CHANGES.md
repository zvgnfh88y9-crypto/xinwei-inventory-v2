# 鑫威公司管理系统｜第二阶段整改说明

本阶段目标：把原来分散存在的“销售订单、生产、质检、库存、出货”真正串成一个可追踪的业务闭环，同时保持第一阶段安全/权限/库存桥接逻辑不被破坏。

## 已完成

### 1. 销售订单锁库改为可重复执行但不会重复占用

- `v2_sales_order_lines` 新增 `locked_qty`。
- `v2_lock_inventory` 改为幂等逻辑：只锁定“尚未发货、尚未锁定”的剩余数量。
- 库存不足时不再整单失败：能锁多少先锁多少，并返回每个 SKU 的缺口。
- 被锁库存使用销售订单 UUID 作为 `batch_no`，形成库存归属，避免不同客户订单共用一笔模糊的 locked 余额。
- 若数据库里存在第一阶段留下的“无订单归属 locked 余额”，`0016` 会主动停止迁移，要求人工核对，系统不会猜测库存属于哪个订单。

### 2. 销售订单页面增加业务闭环状态

现在可以直接看到：

- 订货数量
- 已锁定数量
- 在产数量
- 已发数量
- 待排产缺口

管理员/仓库主管可以：

- 锁定当前可用现货
- 对已锁定数量执行出库
- 查看自动生成的出货单
- 确认客户签收

新增/规范状态：`confirmed`、`in_production`、`ready_to_ship`、`partially_shipped`、`completed`、`cancelled`。

### 3. 生产工单可以绑定具体销售订单明细

`v2_production_orders` 新增 `sales_order_line_id`。

生产页面现在可从“销售订单缺口”直接选择：

- 自动带出销售订单
- 自动带出 SKU
- 自动带出当前未排产缺口
- 自动带出销售交期

后端会校验：

- 关联 SKU 必须一致
- 销售订单/订单明细必须一致
- 已经存在的在产数量会从缺口中扣除
- 不允许重复创建超出当前缺口的关联工单

### 4. 生产报工增加超计划保护

`v2_complete_production` 现在会阻止：

- 负数报工
- 0 数量报工
- 累计良品 + 生产报废超过工单计划数量

生产良品仍先进入 `inspecting`，不会绕过质检直接成为可用库存。

### 5. 质检完成后自动回补销售订单

生产工单如果关联了销售订单明细：

1. 完工良品进入 `inspecting`；
2. 质检合格数量进入 `available`；
3. 系统计算该销售订单还需要多少；
4. 从本批合格库存中自动转到 `locked`；
5. `locked` 使用销售订单 UUID 作为归属标记；
6. 超过客户订单需求的合格品仍保留为正常可用库存。

质检不良品进入 `defective`，不会被自动锁给订单。

质检页同时增加：

- 合格/不良数量边界限制
- 合格 + 不良必须等于待检数量的前端校验
- 有不良品时必须填写不良原因
- 同一收货单重复质检的后端保护

### 6. 增加正式出货/签收状态

新增：

- `v2_shipments`
- `v2_shipment_lines`
- `v2_ship_sales_order`
- `v2_confirm_shipment_delivery`

出货时：

- 只允许使用该销售订单自己锁定的库存；
- `locked` 转为 `shipped`（已发货待签收）；
- 自动增加销售订单行 `shipped_qty`；
- 自动生成出货单号；
- 全部发货后销售订单显示“已全部出货”。

客户确认签收后：

- 对应 `shipped` 状态库存清零；
- 出货单变为 `delivered`；
- 不会再次影响仓库实物库存，因为实物在发货时已经离开仓库。

### 7. 权限保持一致

- `staff`：可以录入/查看销售订单、查看生产信息；不能执行锁库、生产领料、生产报工、质检、出货、签收。
- `inv_manager`：可以执行仓库/生产/质检/出货业务动作。
- `admin`：包含以上能力，并保留系统级管理能力。

生产页面也同步隐藏了普通员工无法执行的操作按钮，避免“按钮能看到但后端拒绝”的体验问题。

## 新增迁移

- `supabase/migrations/0016_sales_production_qc_shipping_loop.sql`

必须在 `0014`、`0015` 之后执行。

## 新增预检

```bash
npm run preflight
```

会检查：

- 敏感文件 / `node_modules` 是否进入交付包
- `run_migration` / `reset_system_passwords` 是否存在可执行分支
- 0014 / 0015 / 0016 是否存在
- 第二阶段关键 RPC 是否定义
- Edge Function 关键 action 是否存在
- 前端闭环 API / 页面入口是否存在
