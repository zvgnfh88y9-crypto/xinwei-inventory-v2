# 鑫威公司管理系统 — 第一阶段整改说明

本版本针对原 Accio Work 项目进行第一阶段安全、权限、数据库和库存一致性整改。

## 已完成

- 移除 WMS Edge Function 中的在线 SQL 执行与核心账号密码重置动作。
- 修复首次登录强制改密缺少 `updatePassword` 导入的问题。
- 登录角色统一为：系统管理员 `admin`、仓库主管 `inv_manager`、普通员工 `staff`。
- 系统管理仅管理员访问；审核、质检、异常、报表由管理员/仓库主管访问。
- 普通员工只能修改自己上传的原始 OCR 凭证；主管/管理员可跨用户处理。
- 原始凭证签名链接增加所有权校验。
- 产品资料编辑、Excel 批量导入不再覆盖已有 SKU 的库存数量。
- 新增产品图片上传后端动作并补充图片格式/大小校验。
- 补齐 `logAudit`，避免主营业务成功后因审计函数缺失继续报错。
- 删除产品前强制检查库存余额与历史流水，防止破坏追溯链。
- 工作流增加创建人/角色权限校验：普通员工只能修改、提交、删除自己的未入账单据；主管/管理员负责审核、入账、红冲。
- 新增迁移 `0014_stage1_security_roles_inventory.sql`：角色模型、账户状态、工作流字段、审计/导入表、统一入账与红冲逻辑。
- 新增迁移 `0015_wms_v2_schema_and_inventory_bridge.sql`：补齐 WMS V2 可重建表结构，并建立旧库存与 V2 状态库存的兼容桥接。
- 旧库存正式流水会同步到 V2 状态库存；V2 状态余额会回写旧页面所需的汇总库存，减少双系统数据漂移。
- 生产完工良品改为先进入“待检库存”，自动生成待质检收货记录；质检合格后才进入“可用库存”，不良转“不良品”。
- 质检加入重复入账/待检库存不足保护。
- 移除前端仍指向不存在后端动作的两个未使用 API 导出。

## 部署前必须做

1. **先完整备份当前 Supabase 数据库。** `0014`/`0015` 会改变权限、函数、触发器和库存同步方式。
2. 在测试/预发布 Supabase 先按顺序执行 migration，再部署三个 Edge Functions：
   - `inventory-action`
   - `workflow-action`
   - `wms-v2-action`
3. 验证重点：现有库存总量、零售仓库存、已入账单据、红冲、生产领料、生产完工、质检入库。
4. 旧项目包中的 `token.txt`、测试默认密码脚本、旧 `deploy_wms_v2.json` 不应继续使用；本交付包未包含这些文件。
5. `.env.local` 未包含在本交付包中。请用 `.env.example` 配置环境变量，不要把真实 Token、Service Role Key、用户密码提交到项目文件或 Git。

## 本地运行

```bash
npm ci
npm run dev
```

生产构建：

```bash
npm run build
```

> 原上传包自带的是 macOS ARM `node_modules`。本交付包已移除 `node_modules`，请在实际开发/部署机器重新执行 `npm ci`，避免跨平台 esbuild 错误。

## 当前仍留到第二阶段处理的内容

- 销售订单 → 生产计划 → 出货的完整自动状态闭环。
- 采购/来料通知与正式收货模块。
- V2 状态库存完整不可变流水账与更细粒度批次成本。
- 异常中心尚未接通的操作按钮。
- 报表中心按批次/真实入库日期计算库龄、呆滞库存和周转率。
- 更完整的单据追溯链（当前 V2 trace 仍是基础版）。
